# DailyMood 3.0 🌟

**DailyMood** — веб-додаток для трекінгу настрою з журналом, цілями, статистикою та магазином wellness-ресурсів.

## ✨ Основні Функції

### 🎯 Основні
- **Журнал настрою** — фіксуйте свій настрій щодня (😊 happy, 😐 neutral, 😢 sad)
- **Статистика** — візуалізація трендів за місяць, розподіл настроїв
- **Цілі та звички** — трекінг щоденних звичок та місячних цілей
- **Мотиваційні цитати** — щоденні цитати для натхнення
- **Темізація** — 7 безкоштовних тем + темна/світла схема

### 💎 Premium Функції
- **Mood Predictor** 🔮 — ML прогноз настрою на завтра на основі патернів
- **Activity Recommendations** 🎯 — персоналізовані поради активностей за настроєм
- **Premium теми** — 10 ексклюзивних тем (purple, teal, sunset, galaxy, forest, crimson, deepsea, gold, lavender, coral, amber)
- **Premium аватари** — 8 magical creatures (unicorn, dragon, koi, phoenix, crown, crystal, moon, butterfly)

### 🛍️ Магазин
- Premium підписка (99 грн)
- Пакети мотиваційних цитат
- Теми оформлення
- Шаблони журналу
- Курси з продуктивності

## 🚀 Швидкий Старт

### Вимоги
- Python 3.9+
- pip

### Встановлення

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/Yarik-eng/DailyMood.git
cd DailyMood
```

2. **Встановіть залежності:**
```bash
pip install -r requirements.txt
```

3. **Ініціалізуйте базу даних:**
```bash
python scripts/init_db.py
```

4. **Додайте тестові продукти (опціонально):**
```bash
python scripts/seed_products.py
```

5. **Запустіть сервер:**
```bash
python app.py
```

або на Windows:
```bash
run.bat
```

6. **Відкрийте у браузері:**
```
http://localhost:5000
```

### Перший запуск
- Перший зареєстрований користувач автоматично отримує права **адміністратора**
- Доступ до адмін-панелі: `/admin/dashboard`

## 📁 Структура Проєкту

```
DailyMood3.0/
├── app.py                  # Головний Flask додаток (1800+ рядків)
├── models.py               # SQLAlchemy моделі (7 таблиць)
├── habits_models.py        # Моделі для звичок та цілей
├── requirements.txt        # Python залежності
├── ARCHITECTURE.md         # Детальна документація архітектури
├── run.bat                 # Windows launcher
│
├── scripts/
│   ├── init_db.py         # Ініціалізація БД
│   ├── create_admin.py    # Створення адміністратора
│   └── seed_products.py   # Тестові продукти
│
├── templates/              # Jinja2 шаблони (18 HTML файлів)
│   ├── base.html          # Базовий шаблон + система темізації
│   ├── index.html         # Головна + Activity Recommendations
│   ├── journal.html       # Журнал настрою
│   ├── statistics.html    # Статистика + Mood Predictor
│   ├── goals.html         # Цілі та звички
│   ├── store.html         # Магазин
│   ├── profile.html       # Профіль користувача
│   ├── login.html         # Вхід
│   ├── register.html      # Реєстрація
│   └── admin_*.html       # Адмін-панелі (5 файлів)
│
├── static/
│   ├── style.css          # Головні стилі
│   ├── script.js          # Головний JavaScript
│   ├── css/
│   │   ├── goals.css
│   │   └── transitions.css
│   ├── js/
│   │   └── i18n_fixed.js  # Інтернаціоналізація
│   └── translations/
│       ├── uk_fixed.js    # Українські переклади
│       ├── en_fixed.js    # Англійські переклади
│       └── quotes.js      # База мотиваційних цитат
│
└── data/                   # SQLite БД (gitignored)
```

## 🗄️ База Даних

### Моделі:
1. **User** — користувачі (email, password_hash, is_admin, is_premium, avatar)
2. **MoodEntry** — записи настрою (mood, date, title, content, activities) 🔒 **прив'язані до користувачів**
3. **Product** — товари магазину
4. **Order** → **OrderItem** — замовлення
5. **Payment** — інформація про оплату
6. **Feedback** — відгуки користувачів
7. **Habit** + **HabitCompletion** — звички
8. **MonthlyGoal** — місячні цілі

### Зв'язки:
- `User` (1) → (N) `Order`
- `User` (1) → (N) `MoodEntry` 🆕
- `Order` (1) → (N) `OrderItem`
- `Order` (1) ↔ (1) `Payment`
- `Habit` (1) → (N) `HabitCompletion`

## 🔧 API Endpoints

### Публічні
- `POST/GET /auth/register` — реєстрація
- `POST/GET /auth/login` — вхід
- `POST /auth/logout` — вихід
- `GET /api/products` — список продуктів

### Авторизовані (@login_required)
- `GET /api/me` — дані поточного користувача
- `GET /api/journal` — список записів настрою (тільки свої) 🔒
- `POST /api/journal` — створити запис
- `PUT /api/journal/<id>` — оновити запис (тільки свій) 🔒
- `DELETE /api/journal/<id>` — видалити запис (тільки свій) 🔒
- `GET /api/journal/export` — експорт у CSV/JSON
- `GET /api/statistics` — статистика за місяць
- `POST /api/orders` — створити замовлення
- `POST /api/payments` — оплатити замовлення
- `GET /api/habits` — список звичок
- `POST /api/habits` — створити звичку
- `POST /api/habits/<id>/toggle` — позначити виконання
- `DELETE /api/habits/<id>` — видалити звичку
- `GET /api/goals` — список цілей
- `POST /api/goals` — створити ціль

### Premium (@login_required + is_premium)
- `GET /api/premium/mood-predictor` — прогноз настрою на завтра
- `GET /api/premium/activity-recommendations` — рекомендації активностей

### Адміністративні (@admin_required)
- `GET /admin/dashboard` — головна панель
- `GET /admin/products|orders|users|feedback` — управління
- `POST /api/products` — створити продукт
- `PUT /api/products/<id>` — оновити продукт
- `DELETE /api/products/<id>` — деактивувати продукт
- `PUT /api/admin/users/<id>/admin` — призначити адміна
- `PUT /api/admin/users/<id>/premium` — активувати Premium
- `DELETE /api/admin/users/<id>` — видалити користувача
- `DELETE /api/feedback/<id>` — видалити відгук

## 🎨 Кастомізація

### Теми профілю:
**Безкоштовні (7):**
- default (індиго)
- pink (рожевий)
- green (зелений)
- rose (червоно-рожевий)
- orange (помаранчевий)
- mint (м'ятний)
- slate (сірий)

**Premium (10):**
- purple (фіолетовий)
- teal (бірюзовий)
- sunset (захід сонця)
- galaxy (галактика)
- forest (ліс)
- crimson (багряний)
- deepsea (глибоке море)
- gold (золотий)
- lavender (лавандовий)
- coral (кораловий)
- amber (бурштиновий)

### Аватари:
**Free (11):** cat, dog, bunny, bear, koala, bee, penguin, frog, mushroom, star, cactus

**Premium (8):** unicorn, dragon, koi, phoenix, crown, crystal, moon, butterfly

## 🛡️ Безпека

### Реалізовано:
- ✅ Хешування паролів (bcrypt)
- ✅ Session-based аутентифікація
- ✅ Перевірка власності записів (user_id)
- ✅ Захист адмін-роутів (@admin_required)
- ✅ Валідація довжини пароля (мін. 6 символів)
- ✅ SQL-injection захист (SQLAlchemy ORM)
- ✅ Гарантія наявності хоча б одного адміна

### Обмеження (локальне середовище):
- ❌ Без CSRF токенів (для локального використання)
- ❌ Без email верифікації
- ❌ Без rate limiting
- ❌ Симуляція оплати (без реального API)

## 💡 Особливості Реалізації

### ✅ Виправлені Проблеми (Листопад 2025):
1. **MoodEntry прив'язані до користувачів** — кожен бачить тільки свої записи
2. **Виправлено create_habit()** — тепер справді створює звички (був копіпаста з login)
3. **Виправлено delete_habit()** — тепер справді видаляє звички
4. **Додано автоматичну міграцію** — старі записи без user_id присвоюються першому користувачу
5. **Додано перевірки власності** — неможливо редагувати/видаляти чужі записи

### Premium Активація:
- Автоматична при покупці продуктів з "premium/преміум" у назві
- Для цифрових продуктів: `order.status = 'completed'` одразу
- Для фізичних: `order.status = 'processing'`

### Payment Flow:
1. Користувач додає товари у кошик
2. Створюється замовлення (`Order` + `OrderItem`)
3. Обирається метод оплати (card/online_banking/paypal)
4. Створюється `Payment` з унікальним `transaction_id`
5. Якщо продукт Premium → активація `user.is_premium = True`

## 📊 Статистика та Аналітика

### Доступно всім:
- Розподіл настроїв за місяць (pie chart)
- Тренд настроїв за 30 днів (line chart)
- Найчастіший настрій
- Середній настрій
- Кількість записів

### Premium:
- **Mood Predictor** — аналіз патернів за 30 днів:
  - Прогноз настрою на завтра
  - Тренд (up/down/stable)
  - Patterns по днях тижня
  - Персоналізовані інсайти
- **Heatmap** — візуалізація настроїв за рік

## 🔮 Mood Predictor Алгоритм

1. Збирає дані за останні 30 днів
2. Аналізує:
   - Загальний розподіл настроїв
   - Тренд останніх 7 днів vs попередніх 7
   - Паттерни по днях тижня
3. Прогнозує настрій на завтра на основі:
   - Історії цього дня тижня (якщо є ≥2 записів)
   - Загального тренду
4. Генерує інсайти та поради

## 🎯 Activity Recommendations

Рекомендує активності залежно від настрою:

**Happy (😊):**
- Творчість, активний спорт, зв'язок з друзями
- Tip: "Використай цю позитивну енергію!"

**Neutral (😐):**
- Читання, медитація, музика, кулінарія
- Tip: "Ідеальний час для саморефлексії"

**Sad (😢):**
- Розслаблююча ванна, journaling, час з улюбленцем
- Tip: "Це тимчасово, ти не одинокий/а"

## 🌐 Інтернаціоналізація

- Українська мова (за замовчуванням)
- Англійська мова (перемикач у навігації)
- Система i18n з `data-i18n` атрибутами
- localStorage для збереження вибору

## 📖 Додаткова Документація

Детальна документація доступна у файлі [`ARCHITECTURE.md`](ARCHITECTURE.md):
- Повний опис моделей БД
- API специфікація
- Бізнес-логіка
- Файлова структура
- Інструкції з розширення

## 🤝 Внесок

Проєкт розробляється для локального використання командою.

## 📝 Ліцензія

Проєкт для освітніх цілей.

## 👨‍💻 Автор

Yarik-eng — [GitHub](https://github.com/Yarik-eng/DailyMood)

---

**Версія:** 3.0  
**Останнє оновлення:** Листопад 2025

🌟 Щасливого трекінгу настрою! 🌟
