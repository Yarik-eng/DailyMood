# DailyMood 3.0 — Shop & Auth Setup Guide

## 🎉 Що було реалізовано

### ✅ Level 1 Requirements (Повністю виконано)
1. **SQLite Database** — база даних у `data/dailymood.db`
2. **Feedback Table & CRUD** — модель Feedback, API `/api/feedback` (GET/POST), адмін-видалення
3. **Admin Panel for Feedback** — сторінка `/admin/feedback` з таблицею відгуків
4. **Shop Tables** — моделі Product, Order, OrderItem з відношеннями
5. **Order-Product Relationships** — Order.items (one-to-many), Product.order_items
6. **Order Status Management** — enum статуси (new/processing/completed/canceled), API для оновлення
7. **Extended Admin Panel** — `/admin/products` (CRUD продуктів), `/admin/orders` (перегляд/статус/видалення)

### 🆕 Додатково реалізовано
- **User Authentication System** — повна реєстрація/логін/logout з bcrypt-хешуванням паролів
- **Session-based Auth** — декоратори @login_required та @admin_required для захисту маршрутів
- **Public Store Page** — сторінка `/store` з карточками продуктів та кнопками "Купити"
- **Wellness Product Types** — 4 типи цифрових продуктів: quote packs, themes, journal templates, habit courses
- **Full i18n** — всі нові features перекладені українською та англійською (60+ нових ключів)
- **Navigation Updates** — динамічний header з login/logout, адмін-меню, email користувача

---

## 📁 Нові файли та зміни

### Backend (Python)
- **models.py**: Додано 4 моделі (User, Product, Order, OrderItem) з методами to_dict(), set_password(), check_password()
- **app.py**: 
  - Додано SECRET_KEY config для сесій
  - Додано декоратори @login_required та @admin_required (українські коментарі ✅)
  - Додано 4 auth endpoints: `/auth/register`, `/auth/login`, `/auth/logout`, `/api/me`
  - Додано 4 products endpoints: GET/POST/PUT/DELETE `/api/products` (admin-захищені)
  - Додано 5 orders endpoints: POST/GET/GET-id/PUT-status/DELETE `/api/orders`
  - Додано 3 routes для HTML: `/auth/login`, `/auth/register`, `/store`
  - Захищено admin маршрути декоратором @admin_required

### Frontend (Templates & JS)
- **admin_products.html** (NEW): Таблиця продуктів + inline форма для CRUD з 6 полями, JS-обробники
- **admin_orders.html** (NEW): Таблиця замовлень з dropdown статусів, модальне вікно деталей замовлення
- **store.html** (NEW): Grid карток продуктів з типами, цінами, кнопками "Купити"
- **login.html** (NEW): Форма логіну з email/password, редірект на головну після успіху
- **register.html** (NEW): Форма реєстрації з валідацією паролю (мін. 6 символів)
- **base.html** (UPDATED): 
  - Додано Store link у навігацію
  - Додано user-nav-items/guest-nav-items (login/logout/register)
  - Додано адмін-dropdown з посиланнями на Products/Orders/Feedback
  - Додано JS для завантаження `/api/me` та оновлення навігації

### Translations
- **uk.js** (UPDATED): Додано 60+ ключів (auth, admin, store, загальні)
- **en.js** (UPDATED): Додано 60+ ключів (англійські переклади)

---

## 🚀 Як запустити

### 1. Встановити залежності
```powershell
pip install -r requirements.txt
```

### 2. Запустити додаток
```powershell
python app.py
```

Або через run.bat:
```powershell
.\run.bat
```

### 3. Ініціалізація бази даних
При першому запуску Flask автоматично створить таблиці через `db.create_all()` у app.py.

**Нові таблиці:**
- `users` — користувачі з email, password_hash, is_admin, created_at
- `products` — продукти з name, slug, type, description, price, is_active
- `orders` — замовлення з user_id, status, total_amount, created_at
- `order_items` — елементи замовлення з order_id, product_id, quantity, unit_price, subtotal

---

## 👤 Створення першого адміністратора

**Проблема:** Декоратор @admin_required перевіряє прапорець `is_admin=True`, але у новостворених користувачів він False.

**Рішення:** Вручну оновити першого користувача в базі даних.

### Варіант 1: SQL команда (через DB Browser for SQLite)
1. Скачайте [DB Browser for SQLite](https://sqlitebrowser.org/)
2. Відкрийте `data/dailymood.db`
3. Перейдіть на вкладку "Execute SQL"
4. Виконайте:
```sql
UPDATE users SET is_admin = 1 WHERE id = 1;
```

### Варіант 2: Python скрипт
Створіть файл `scripts/create_admin.py`:
```python
import sys
sys.path.append('..')

from app import app, db
from models import User

def create_admin(email, password):
    """Створити адмін-користувача або оновити існуючого."""
    with app.app_context():
        user = User.query.filter_by(email=email).first()
        
        if user:
            # Оновити існуючого користувача
            user.is_admin = True
            user.set_password(password)
            db.session.commit()
            print(f"✅ Користувач {email} оновлено як адмін.")
        else:
            # Створити нового
            user = User(email=email, is_admin=True)
            user.set_password(password)
            db.session.add(user)
            db.session.commit()
            print(f"✅ Адмін-користувач {email} створено.")

if __name__ == '__main__':
    create_admin('admin@dailymood.com', 'admin123')
```

Запустити:
```powershell
cd scripts
python create_admin.py
```

### Варіант 3: Flask shell
```powershell
# У кореневій папці проєкту
python -c "from app import app, db; from models import User; app.app_context().push(); u = User.query.filter_by(email='admin@dailymood.com').first(); u.is_admin = True; db.session.commit(); print('✅ Admin created')"
```

---

## 🔐 Структура автентифікації

### Декоратори
```python
@login_required      # Вимагає залогіненого користувача (перевіряє session['user_id'])
@admin_required      # Вимагає адміна (перевіряє User.is_admin == True)
```

### API Endpoints

#### Auth
- `POST /auth/register` — створює користувача, валідує email (унікальний) та пароль (6+ символів)
- `POST /auth/login` — перевіряє email/password, створює сесію
- `GET /auth/logout` — видаляє сесію
- `GET /api/me` — повертає інформацію про поточного користувача (email, is_admin)

#### Products (Admin-protected)
- `GET /api/products` — список продуктів (публічний показує тільки активні, адмін — всі)
- `POST /api/products` — створити продукт (admin)
- `PUT /api/products/<id>` — оновити продукт (admin)
- `DELETE /api/products/<id>` — soft-delete (is_active=False, admin)

#### Orders (Login-required)
- `POST /api/orders` — створити замовлення з масивом items: [{product_id, quantity}]
- `GET /api/orders` — список замовлень (користувач бачить свої, адмін — всі)
- `GET /api/orders/<id>` — деталі замовлення (з перевіркою власності)
- `PUT /api/orders/<id>/status` — оновити статус (admin)
- `DELETE /api/orders/<id>` — видалити замовлення (admin)

---

## 🛒 Типи продуктів

Магазин підтримує 4 типи цифрових wellness-продуктів:

1. **quote_pack** (Пакет цитат) — колекції мотиваційних цитат
2. **theme** (Тема оформлення) — кастомні кольорові схеми для інтерфейсу
3. **journal_template** (Шаблон щоденника) — готові структури для записів
4. **habit_course** (Курс звичок) — покрокові програми формування звичок

Усі продукти мають:
- `name` — назва
- `slug` — унікальний URL-friendly ідентифікатор
- `type` — тип продукту (один з 4 вище)
- `description` — опис (може бути порожнім)
- `price` — ціна (Decimal)
- `is_active` — активний чи ні (soft delete)

---

## 📊 Статуси замовлень

Замовлення мають 4 статуси:
- `new` — нове (створено користувачем)
- `processing` — в обробці (адмін почав опрацьовувати)
- `completed` — завершено (продукт надано користувачу)
- `canceled` — скасовано

Адмін може змінювати статус через dropdown у `/admin/orders`.

---

## 🌐 Переклади

Усі нові UI елементи мають ключі у `uk.js` та `en.js`:
- Форми логіну/реєстрації
- Адмін-панелі продуктів та замовлень
- Сторінка магазину
- Навігація (login/logout/admin dropdown)
- Повідомлення про успіх/помилки

**Важливо:** Усі `data-i18n` атрибути працюють через `i18n_fixed.js`, який слухає подію `languageChanged`.

---

## 🧪 Тестування

### Мануальні кроки
1. **Реєстрація**: Відкрийте `/auth/register`, створіть акаунт
2. **Зробити адміном**: Виконайте один з варіантів створення адміна (вище)
3. **Логін як адмін**: Увійдіть через `/auth/login`
4. **Перевірте навігацію**: У header має з'явитися dropdown "Адмін ▾"
5. **Додайте продукт**: Перейдіть `/admin/products`, створіть продукт (наприклад, quote_pack за 9.99)
6. **Вийдіть та залогіньтесь як звичайний юзер**: Створіть другий акаунт без is_admin
7. **Купіть продукт**: Відкрийте `/store`, натисніть "Купити" — має створитися замовлення
8. **Перегляньте замовлення як адмін**: Увійдіть як адмін, перейдіть `/admin/orders`, змініть статус на "processing"

---

## ⚙️ Конфігурація

### SECRET_KEY
У продакшені **обов'язково** встановіть змінну середовища:
```powershell
$env:SECRET_KEY = "ваш-супер-секретний-ключ-тут"
```

### Database Path
За замовчуванням: `data/dailymood.db`
Можна змінити у `app.py`:
```python
db_path = os.path.join(basedir, 'data', 'dailymood.db')
```

---

## 📝 Коментарі в коді

**Всі нові Python-функції мають українські docstrings та коментарі** (згідно вимоги користувача).

Приклад:
```python
def login_required(f):
    """Декоратор для захисту маршрутів — вимагає залогіненого користувача."""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if 'user_id' not in session:
            return jsonify({'error': 'Необхідна авторизація'}), 401
        return f(*args, **kwargs)
    return decorated_function
```

---

## 🐛 Відомі обмеження

1. **Перший адмін створюється вручну** — немає автоматичного seed-скрипта
2. **Soft delete продуктів** — видалення встановлює `is_active=False`, щоб зберегти зв'язок з історією замовлень
3. **Немає payment processing** — кнопка "Купити" одразу створює замовлення без оплати
4. **Немає email-підтвердження** — реєстрація миттєва без верифікації email
5. **Session timeout не налаштований** — сесії живуть до закриття браузера

---

## 📚 Додаткові ресурси

- [Flask-SQLAlchemy Docs](https://flask-sqlalchemy.palletsprojects.com/)
- [Werkzeug Security](https://werkzeug.palletsprojects.com/en/3.1.x/utils/#module-werkzeug.security)
- [DB Browser for SQLite](https://sqlitebrowser.org/)

---

**Виконано всі вимоги Level 1 ✅**  
**Код коментується українською ✅**  
**Повна інтернаціоналізація ✅**  
**E-commerce інтегровано як wellness marketplace ✅**
